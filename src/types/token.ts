export interface IndexerToken {
  address: string
  chain: string
  decimals: number
  name: string
  symbol: string
}

export interface CoingeckoToken {
  id: string
  symbol: string
}

export interface CmcToken {
  symbol: string
  slug: string
}
